We should use NUnit and NUnit alone, use the latest version and its latest APIs
We should avoid FluentValidation or any such third party libraries (FsCheck), NUnit based assertions
should be good enough, using all such additional libraries are brinding technology complexity
and burning AI credits and we dont want to do any of these

Always run the tests after doing major task to ensure we havnt broken anything
