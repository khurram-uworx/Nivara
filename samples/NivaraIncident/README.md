# NivaraIncident — Production Incident Replay and Investigation on a Columnar Engine

A reference application that models a production telemetry environment (services, requests,
deployments, errors, latency, infrastructure signals, dependency relationships), replays a
historical incident as if it were happening live, and investigates it through Nivara's typed,
columnar, fused analytical pipeline — all in native .NET.

**Target audience:** engineers who want to see whether a serious production-grade analytical
application can be built on top of Nivara, and the Nivara team itself, which uses this sample as
a forcing function to discover missing capabilities and performance gaps.

> The objective is not to build another dashboard demo. It is to answer:
> *Could a serious .NET engineer build a production-grade analytical application on top of Nivara?*
> — and in the process make the library better.

## What it does

Given a deterministic telemetry snapshot (10M+ requests, 50+ endpoints, 10 regions, 100+ service
instances, logs, traces, deployments), NivaraIncident reconstructs the incident timeline and
answers questions like:

- Which service degraded first?
- Which deployment correlates with the degradation?
- Which endpoints contributed most to failures?
- How did latency evolve over time?
- Did retries propagate the incident? What was the blast radius?

It ships **reproducible incident scenarios** (no random failures) generated by a deterministic
dataset generator:

| Incident | Story | Key analyses |
|----------|-------|--------------|
| **A — Database degradation** | PostgreSQL latency → orders → checkout → payment timeout → retry storm | which service degraded first, downstream propagation delay, retry amplification |
| **B — Bad deployment** | orders-api v4.21 deployed → exception rate ↑ → failures ↑ | deployment precedes degradation?, affected endpoints, time-to-customer-impact |
| **C — Traffic spike** | traffic ×8 → queue depth ↑ → latency ↑ → timeouts ↑ | which services saturated first, which recovered first |
| **D — Regional failure** | us-east/eu-west healthy, ap-south degraded | partitioning, grouping, ranking by region |

Three surfaces:

1. **CLI** — `dotnet run --project samples/NivaraIncident.Cli generate|analyze|replay` for
   engineers and benchmarks. `generate` produces deterministic Parquet/CSV datasets; `analyze`
   runs all five analyses (degradation ordering, deployment correlation, saturation ordering,
   regional partitioning, grouped aggregation) with optional chunked streaming; `replay`
   replays Parquet row groups with chunk-by-chunk progress.
2. **Web UI** — ASP.NET Core dashboard (planned, Milestone 2).
3. **Library API** — the underlying analyses stay visible as normal Nivara query code so the
   sample teaches the API rather than hiding it.

The killer demo is **Incident Replay**: historical Parquet analysis and live/replay analysis
converge on the same Nivara pipeline (`IAsyncEnumerable<Chunk>` → Nivara plan → fused operators →
bounded buffers → CollectAsync).

## Nivara capabilities exercised

The sample is written to exercise real Nivara capabilities, not hide behind LINQ-to-objects:

- **Typed expression execution** — `Filter`/`Select`/`Sort` via `ColumnExpressions.Col`/`Lit`,
  fused into kernel passes by the ADR-004 expression engine.
- **Rolling windows** — `RollingMean` for error-rate computation, `RollingMax` for queue-depth
  saturation tracking.
- **Shift / lead** — `Shift` for delta-since-previous-interval analyses (error-rate deltas).
- **Rank family** — `PercentRank` partitioned by region for regional incident analysis.
- **Typed LINQ GroupBy** — `NivaraQuery<T>.GroupBy().Select()` with `Count()`, `Sum()`,
  `Average()` for grouped aggregation.
- **Chunked / streaming execution** — `AsStream()` over Parquet row groups with
  `IAsyncEnumerable` and `IAsyncDisposable` for bounded replay.
- **Parquet / CSV ingestion** — `Parquet.ScanAsQueryFrame` and `Csv.ScanAsQueryFrame` for
  chunk-capable data sources.
- **`IAsyncDisposable`** — proper resource cleanup on `QueryFrame` instances.

## Nivara APIs demonstrated

| API | Where | Status |
|-----|-------|--------|
| `QueryFrame.Filter/Select/Sort` | Analysis layer | ✅ exercised |
| `QueryFrame.RollingMean/RollingMax` | Analysis layer | ✅ exercised |
| `QueryFrame.Shift` | Analysis layer | ✅ exercised |
| `QueryFrame.PercentRank` | Analysis layer | ✅ exercised |
| `QueryFrame.Collect` | Analysis / replay | ✅ exercised |
| `NivaraQuery<T>.GroupBy/Select/Count/Sum/Average` | Grouped aggregation | ✅ exercised |
| `QueryFrame.AsStream(int chunkSize, CancellationToken)` | Replay engine | ✅ exercised |
| `IAsyncDisposable` | Replay engine | ✅ exercised |
| `Parquet.ScanAsQueryFrame` | Ingestion | ✅ exercised |
| `Csv.ScanAsQueryFrame` | Ingestion | ✅ exercised |
| `NivaraFrame.Create` | Result construction | ✅ exercised |
| `ColumnExpressions.Col/Lit` | Expression tree | ✅ exercised |
| `WindowSpec` (`Over()`/`PartitionBy`/`OrderBy`) | Analysis layer | available, not yet used |
| `ColumnExpressions.Rolling*/Rank/DenseRank` | Analysis layer | available, not yet used |
| `NivaraColumn<T>.Diagnostics` / `DiagnosticsTracker` | Diagnostics | available, not yet used |
| `QueryFrame.ExplainPlan()` / `GetExecutionDiagnostics()` | Query-plan view | available, not yet used |

## Architecture (implemented)

The Phase 3a implementation uses a flat library + thin CLI layout:

```
samples/
├── Nivara.Samples/Incident/
│   ├── Schema.cs             # Telemetry record types (RequestTelemetry, DeploymentEvent, etc.)
│   ├── Scenarios.cs           # 4 deterministic incident scenarios (A–D)
│   ├── DatasetGenerator.cs    # 10M+ record generator (Parquet + CSV output)
│   ├── Ingestion.cs           # Parquet/CSV loading wrappers + async streaming
│   └── Analysis.cs            # 5 analysis methods using QueryFrame pipeline
├── NivaraIncident.Cli/
│   ├── NivaraIncident.Cli.csproj
│   └── Program.cs             # CLI: generate | analyze | replay
└── NivaraIncident/
    ├── IDEA.md                # Full product spec
    ├── README.md              # This file
    └── Python/
        ├── gen_reference.py   # Polars rank/rolling reference fixtures
        └── requirements.txt
```

Core principle: **Every important number shown in the UI should be calculated by Nivara.**
The sample is deliberately standalone-first: `dotnet run`, then optionally `docker run` — no
database, no Kafka, no cloud account.

## Files

| File | Purpose |
|------|---------|
| `samples/Nivara.Samples/Incident/Schema.cs` | Telemetry record types (request, deployment, dependency, instance) |
| `samples/Nivara.Samples/Incident/Scenarios.cs` | 4 deterministic incident scenarios with event timelines |
| `samples/Nivara.Samples/Incident/DatasetGenerator.cs` | Deterministic 10M+ record generator with Box-Muller latency |
| `samples/Nivara.Samples/Incident/Ingestion.cs` | Parquet/CSV loading wrappers and async streaming helper |
| `samples/Nivara.Samples/Incident/Analysis.cs` | 5 analysis methods exercising QueryFrame pipeline |
| `samples/NivaraIncident.Cli/Program.cs` | CLI entry point (generate/analyze/replay) |
| `samples/NivaraIncident/Python/gen_reference.py` | Polars rank/rolling cross-validation fixtures |

## Requirements

- .NET 10.0 SDK
- `src/Nivara/Nivara.csproj` (core; no external dependencies)
- `src/Nivara.Extensions/Nivara.Extensions.csproj` (Parquet/CSV IO, dependency-only packages)
- Optional: Python 3.12 + polars for regenerating rank/rolling cross-validation fixtures

---

## Library gaps this example exposed and resolved

The Incident Lab is deliberately a **forcing function**: when the sample requires an awkward
workaround, the rule is to first ask whether Nivara itself is missing a capability or has an
incorrect abstraction, fix the core, then build the sample on the fixed API.

The gap inventory below is the result of a code audit performed against the current `main`
before implementation starts. Every item is marked with its **verified status**: already fixed
(by prior roadmap work or earlier samples), or open (to be fixed while implementing this sample).

### Already fixed (verified on `main`)

| Capability | Status | Evidence |
|------------|--------|----------|
| **Rank family** (`RowNumber`/`Rank`/`DenseRank`/`PercentRank`) | ✅ done | `RankKernel.Compute` (`src/Nivara/Tensors/RankKernel.cs`), `PartitionedWindowEngine`, public `NivaraFrameExtensions.RowNumber/Rank/DenseRank/PercentRank` (`src/Nivara/WindowFrameExtensions.cs:179-222`), `ColumnExpressions.Rank/DenseRank/PercentRank/RowNumber` (`src/Nivara/Expressions/ColumnExpression.cs:878-896`), Polars cross-validation fixtures in `samples/data/polars-window/manifest.json` + `tests/Nivara.Tests/Query/PolarsWindowCrossValidationTests.cs` (issue #254 semantics documented). |
| **Rolling / cumulative windows + shift/lead** | ✅ done | `NivaraFrameExtensions.RollingSum/RollingMean/RollingMin/RollingMax/Cumulative*`/`Shift`/`Lead` (`src/Nivara/WindowFrameExtensions.cs:40-171`), `ColumnExpressions.Rolling*/Shift/Lead` (`src/Nivara/Expressions/ColumnExpression.cs:811-871`), Polars cross-validation in `tests/Nivara.Tests/Query/RollingWindowCrossValidationTests.cs`. |
| **`Over()`/`WindowSpec` builder** | ✅ done | `NivaraFrameExtensions.Over()` (`src/Nivara/WindowFrameExtensions.cs:29`), `WindowSpec` (`src/Nivara/Operations/WindowSpec.cs`). |
| **Typed fused expressions (ADR-004)** | ✅ done | One `KernelPlan` IR + three backends (`FusedKernel` span interpreter, `TensorPrimitivesKernel` SIMD, compiled delegate), chunked evaluation bit-identical to whole-column (`docs/adr/004-*.md`). |
| **Phase 4 streaming (`CollectAsync`/`AsStream`/`ToListAsync`)** | ✅ done | Public `QueryFrame.AsStream(int chunkSize, CancellationToken)` (`src/Nivara/Query/QueryFrame.cs:459`), `CollectAsync` (`:406`), `NivaraQuery<T>.AsStream` (`src/Nivara/Linq/NivaraQuery.cs:229`), bounded `Channel<T>` producer/consumer, chunk-capable CSV/JSON/Parquet sources, `IAsyncDisposable` on `QueryFrame` (issue #264/#267). Behavior contract in `docs/STREAMING.md`. |
| **`ScanAsQueryFrame` for Parquet/CSV/JSON** | ✅ done | `Parquet.ScanAsQueryFrame` (`src/Nivara.Extensions/IO/NivaraParquetReader.cs:127`), `Csv.ScanAsQueryFrame` (`src/Nivara.Extensions/IO/CsvExtensions.cs:63`), `Json.ScanAsQueryFrame` (`src/Nivara/IO/JsonExtensions.cs:63`). |
| **ADR-001 non-nullable AutoDiff boundary** | ✅ done | Constructors throw `AutoGradException("ADR-001 …")` on null input (`src/Nivara/AutoDiff/ReverseGradTensor.cs:47`, `ForwardGradTensor.cs:38`), zero-copy `FromColumn/FromSeries/FromArray/FromMatrix`, inference-default via `GradientUtils.Grad()`. |
| **AutoDiff ops span-ified + `TensorPrimitives`** | ✅ done | `ReverseGradOperations.cs` / `ForwardGradOperations.cs` / `GradKernels.cs` operate on contiguous spans with `TensorPrimitives`; no `NivaraColumn.Data` access in kernels (ADR-002). |

### Resolved by Phase 1 core gap-fills (branch `khurram/incident`)

The five core library gaps the plan's Phase 1 shipped against were fixed in core on
`khurram/incident` (commits listed; tracked as executed in the plan's 1.6 completion marker).

| # | Capability | Status | Evidence |
|---|------------|--------|----------|
| 1 | **Percentile / quantile / median aggregation** | ✅ resolved | `QuantileAggregation`/`MedianAggregation` in `src/Nivara/Operations/AggregationFunction.cs` + `NivaraSeries<T>.Quantile(q)`/`Median()` (commit `9a4b2b5`). The `ColumnExpressions.Quantile` window/aggregation expression node remains deferred to issue #277 (needed for group→aggregate→rank plans). |
| 2 | **StdDev / Variance aggregation** | ✅ resolved | `StdDevAggregation`/`VarianceAggregation` + `NivaraSeries<T>.StdDev()`/`Variance()` (commit `4f20023`). |
| 3 | **Execution diagnostics reachable through the public query path** | ✅ resolved | Public `QueryFrame.LastExecutionDiagnostics` / `GetExecutionDiagnostics()` with `RowsRead`/`RowsReturned`/`MaterializedColumns` counters instrumented across all four strategies + `GenerateReport()` (commit `17e106f`); the `InternalsVisibleTo` alternative is obsolete. |
| 4 | **Parquet chunk streaming without per-chunk metadata reparse** | ✅ resolved | `ParquetLazySource` reuses a single `ParquetReader` (footer metadata parsed once) guarded by a `SemaphoreSlim`, with true-sync `Execute`/`ReadChunk` and row-group-aligned chunks; `ReadParquetStreaming` yields one frame per row group (commit `2529e63`). |
| 7 | **Streamed row materialization** | ✅ resolved | `NivaraQuery<T>.ToObjectsAsync` per-chunk row projection with constant memory (commit `3e45188`). |

### Open gaps to fix while implementing this sample

These are the items the implementation plan (`../Incident-PLAN.md`) targets. Gaps 1, 2, 3, 4, and
7 above were fixed in core by the Phase 1 branch; the remaining items below are **deferred** to a
follow-up tracked in issue #284 (Phase 2 AutoDiff cleanup/SIMD = gaps 9/10; Phase 3 sample
measurements = gaps 5/6/8), except gap 8's design-test which is escalated to Phase 3. Each was
verified as genuinely missing on `main` by the pre-implementation audit.

#### 5. Window semantics across chunk boundaries (non-streamable fallback) — **DEFERRED to issue #284 (Phase 3 measurement)**

Window expressions (`ColumnExpressions.RowNumber/.Rank/...`),
`Rolling`, `Cumulative`, `Shift`, `Rank`, `Sort`, `GroupBy`, `Join`, `Distinct` are
**non-streamable** (`StreamingExecutionStrategy.NonStreamableOperations`, `:10`). `AsStream` is
all-or-nothing (any boundary op → single merged frame, `docs/STREAMING.md:36-55`); `CollectAsync`
uses flush-concatenate-resume. The sample should **measure** whether window-heavy replay queries
defeat true streaming, and decide (with evidence) whether cross-chunk window computation is a
core improvement worth pursuing.

#### 6. Memory budget is advisory, not a hard limit — **DEFERRED to issue #284 (Phase 3 measurement)**

The bounded `Channel<T>` (capacity clamped to [2,16], `StreamingExecutionStrategy:86`) bounds
in-flight chunks, but there is no byte-level enforcement on the query path. `StreamingBufferManager`
(`src/Nivara.Extensions/IO/StreamingBufferManager.cs:21`) exists but is deliberately **not wired
into** the query strategy (its only caller is the stub `ReadParquetStreamingInternal`). The sample
should stress this with a large replay and report peak memory vs budget.

#### 8. Query API ergonomics for group→aggregate→rank→filter — **EXERCISED in Phase 3a, gap confirmed**

`QueryFrame` has `GroupBy` but no `Aggregate`/`Having`/`Where` alias (`Filter` is the only
predicate, `QueryFrame.cs:108`). The eager `NivaraFrameExtensions.GroupBy(frame, string[],
Dictionary<string, AggregationFunction>)` overload (`NivaraFrameExtensions.cs:1123-1156`) is a
trap: it validates columns, builds `GroupByOperation` **without aggregations**, and returns only
the keys — its own comment calls it a "simplified implementation". The sample works around this
two ways: (a) typed LINQ `frame.Query<T>().GroupBy().Select()` with `Count()`/`Sum()`/`Average()`
for real aggregations, and (b) manual dictionary aggregation after `Collect()`. A cleaner fluent
path would improve ergonomics significantly.

#### 9. ADR-001 residual cleanup (dead branches inside the non-null domain) — **DEFERRED to issue #284 (Phase 2)**

The AutoDiff audit confirms the domain is **fully compliant** — no null-mask propagation remains
in hot paths; all 8 data-null checks are legitimate boundary enforcement. Two cosmetic dead
branches remain and should be removed while touching these files:
- `Gather` (`ReverseGradOperations.cs:2423-2441`): `TryGetSpan` fast path with an `else`
  indexer fallback that is unreachable post-boundary (ADR-001 consequence: `TryGetSpan` always
  succeeds in-domain).
- `BroadcastGradient` (`GradOperationKernels.cs:241-258`): same `TryGetSpan` + `ArrayPool`
  fallback pattern.

#### 10. SIMD opportunities discovered by the audit — **DEFERRED to issue #284 (Phase 2)**

These are performance improvements, not correctness fixes. Prioritized:

| Location | Loop | Opportunity |
|----------|------|-------------|
| `ReverseGradOperations.Pow` (`:1637-1655`) | scalar `Math.Pow` | **Route through existing SIMD `GradOperationKernels.ApplyPow`/`ApplyPowGradient`** (already `TensorPrimitives.Pow`) — the reverse op is the inconsistent outlier vs forward-mode |
| `Adam.cs:125-139` / `AdamW.cs:111-125` | generic scalar fallback | Reached only for types other than float/double/Half; under `IFloatingPointIeee754<T>` all three dispatch to dedicated SIMD kernels (float `:83`, **double `:99`/`:85`**, Half `:111`/`:97`) — the tail loop is effectively defensive, not a live `double` gap |
| `GradOperationKernels.cs:226-227` | RMSNorm grad `g*invRms - x*scale` | `TensorPrimitives` multiply/subtract chain |
| `ReverseGradOperations.cs:2651-2683, 2723-2749` (+ `ForwardGradOperations.cs:1357-1454`) | `BroadcastMultiply`/`BroadcastAdd` channel-strided loops | Per-channel-run `TensorPrimitives` when contiguous |
| `SGD.cs:20-39` | momentum step loop | `TensorPrimitives.MultiplyAdd` chain |

The `Pow` SIMD inconsistency (item 1) is a small, high-value, low-risk win that should land with
a PyTorch-parity regression test (the NivaraTorch fixture suite covers Pow).

---

## Performance goals

The IDEA's illustrative targets, to be validated by the sample's diagnostics view:

```text
Dataset       4.2 GB        Rows 48.9M (streamed in 3 chunks)
Elapsed       2.14 s        Peak memory 412 MB (bounded channel in flight)
Kernels       5 executed    0 per-row boxing
```

The important properties are **bounded memory** during replay (channel backpressure) and
**visible kernel selection** (fused / SIMD vs scalar) in the diagnostics view.

## Limitations

- Phase 3a implements CLI, dataset generation, ingestion, and analysis queries. Web UI and
  Polars cross-validation tests are planned for future milestones.
- `NivaraFrameExtensions.GroupBy(frame, keys, aggregations)` is a **trap** — it returns only
  grouped keys, not aggregation results. The sample uses typed LINQ `GroupBy().Select()` and
  manual aggregation as workarounds (see Gap 8 below).
- `AsStream` window-heavy queries fall back to single-frame materialization (Gap 5) — the replay
  demo may not be truly chunked for window-heavy analyses until this is measured and addressed.

## Future work

1. **Phase 3b (Tests)** — NUnit tests covering scenario determinism, dataset generator output
   shape, ingestion round-trips, analysis query correctness, CLI end-to-end, and streaming.
2. **Milestone 2 (Web UI)** — ASP.NET Core dashboard (timeline, service health, endpoint/regional
   analysis, dependency view, query-plan view); every number computed by Nivara.
3. **Milestone 3 (Replay dashboard)** — `IAsyncEnumerable<Chunk>` live dashboard driven by the
   same analytical pipeline as historical Parquet analysis.
4. **Milestone 4 (Generated schema)** — Roslyn source generator for typed telemetry accessors
   (Phase 5 roadmap), demonstrating `C# schema → generator → typed Nivara accessors → fused/SIMD`.
5. **Milestone 5 (Deployment)** — `dotnet run` and `docker run -p 8080:8080` parity on a
   developer laptop.
6. **Cloud-oriented follow-up** — optional deployment showing the same engine consuming real
   OpenTelemetry streams.

---

For the implementation plan and the engineering-rule escalation path (sample problem vs core
gap), see **`../Incident-PLAN.md`**.
